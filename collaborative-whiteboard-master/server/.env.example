# Server-specific example env for convenience
# Copy to server/.env or set these in your environment when running the Spring Boot app.

# Primary MongoDB connection used by Spring Boot (preferred)
SPRING_DATA_MONGODB_URI= mongodb+srv://ht716756_db_user:<db_password>@board.wdyywdw.mongodb.net/?appName=Board

# Alternatively, for a local docker-compose MongoDB you can set these values
MONGO_INITDB_ROOT_USERNAME=
MONGO_INITDB_ROOT_PASSWORD=

# Optional: maven wrapper creds (if private repo access is required)
MVNW_USERNAME=
MVNW_PASSWORD=
